{
  "name": "visora-render-worker",
  "version": "0.2.0",
  "private": true,
  "description": "Visora render engine — Cloudflare Worker using Browser Rendering (headless Chromium) + R2",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "dependencies": {
    "@cloudflare/puppeteer": "^1.1.0",
    "@supabase/supabase-js": "^2.45.0",
    "nanoid": "^5.0.7"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20251014.0",
    "wrangler": "^4.59.3"
  }
}
