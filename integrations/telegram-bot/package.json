{
  "name": "visora-telegram-bot",
  "version": "0.1.0",
  "private": true,
  "description": "Visora Telegram bot — connect flow + /render command, Cloudflare Worker",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260730.1",
    "typescript": "^5.5.3",
    "wrangler": "^4.59.3"
  }
}
